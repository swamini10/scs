info:
  name: Resolved Complaints
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/api/admin/resolved
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
