info:
  name: Raise Complaint API
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/complaints/2
  body:
    type: json
    data: |-
      {
          "title": "WiFi Issue",
          "description": "College WiFi is not working"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
