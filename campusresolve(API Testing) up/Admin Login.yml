info:
  name: Admin Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/users/login
  headers:
    - name: Authorization
      value: "Bearer "
  body:
    type: json
    data: |-
      {
        "email": "admin@gmail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
