info:
  name: User Login API
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users/login
  body:
    type: json
    data: |-
      {
        "email": "swamini@gmail.com",
        "password": "12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
