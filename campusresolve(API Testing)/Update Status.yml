info:
  name: Update Status
  type: http
  seq: 10

http:
  method: PUT
  url: http://localhost:8080/api/complaints/7?status=RESOLVED
  params:
    - name: status
      value: RESOLVED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
