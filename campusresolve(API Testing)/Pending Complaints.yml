info:
  name: Pending Complaints
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api/admin/pending
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
